FROM oven/bun:1
WORKDIR /app

# 依存関係を先にインストール（キャッシュ層として分離）
COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

# ソースコードは docker-compose の volume mount で上書きされる
EXPOSE 3000
CMD ["bun", "dev", "--hostname", "0.0.0.0"]
